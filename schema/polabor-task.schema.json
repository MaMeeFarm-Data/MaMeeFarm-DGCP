{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PoLABOR Task",
  "description": "Schema for human labor activities in DGCP.",
  "type": "object",
  "properties": {
    "date": { "type": "string", "format": "date" },
    "task": { "type": "string" },
    "duration_minutes": { "type": "number" },
    "notes": { "type": "string" }
  },
  "required": ["date", "task"],
  "additionalProperties": false
}
